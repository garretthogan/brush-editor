{
  "name": "brush-editor",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:run": "vitest run",
    "test:e2e": "PLAYWRIGHT_BROWSERS_PATH=.playwright-browsers playwright test",
    "test:e2e:install": "PLAYWRIGHT_BROWSERS_PATH=.playwright-browsers npx playwright install chromium",
    "test:e2e:ui": "PLAYWRIGHT_BROWSERS_PATH=.playwright-browsers playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "vite": "^7.2.4",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "three": "^0.182.0",
    "three-bvh-csg": "^0.0.18",
    "three-mesh-bvh": "^0.9.7"
  }
}
